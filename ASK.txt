1. getStudent and getStudents show json or string in console?
2. Should RESTClient only handle JSON format?
3. Delete/Update/Insert Student() should work with urls like "param=value&param2=value2" or is it fine with "jsonName={'param':value,'param2':value2}" ? or both?
3. GetStudent(ID) 
